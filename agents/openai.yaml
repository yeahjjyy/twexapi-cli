interface:
  display_name: "Twexapi CLI"
  short_description: "Help with twexapi CLI tasks and workflows"
  default_prompt: "Use $twexapi-cli to install, configure, and run twexapi commands in this repository."

policy:
  allow_implicit_invocation: true
